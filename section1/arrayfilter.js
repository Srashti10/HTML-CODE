const prices = [2300, 499, 233,199,450,3400,1800];
const filteredprice1 = prices.filter( (p) => {return p<500});
console.log(filteredprice1);

const names = ['Raju', 'Kaliya', 'Pinky'];
const filterednames = names.filter((p) => {return p.length>=5});
console.log(filterednames);

//create array of names and filter all the names starting with s.
const names1 = ['srashti','riya','shreoshi','raju','pinki'];
const filterednames1 = names1.filter((n) => {return n.startsWith('s')});
console.log(filterednames1);

//create an array and check the numbers which are perfect square or not





//create an array and filter all prime numbers in the array
